# dia declarado
day = "terca"

# O uso do (==) signfica que estou COMPARANDO se o day (declarado) com "quarta", por exemplo.
# O uso do (!=) signfica o contrario do (==)
if day == "quarta"
   status = "viajo para taquaritinga-PE"
else
    status = "hoje fico em casa"
end

puts "O status de hoje #{status}"
